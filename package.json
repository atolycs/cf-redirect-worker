{
  "name": "cf-redirtect-worker",
  "version": "0.0.7",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types",
    "format": "biome format . --fix",
    "prepare": "husky",
    "lintstaged": "lint-staged"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.8.3",
    "@cloudflare/vitest-pool-workers": "^0.1.0",
    "@cloudflare/workers-types": "^4.20240524.0",
    "husky": "^9.1.4",
    "lint-staged": "^15.2.9",
    "typescript": "^5.0.4",
    "vitest": "1.3.0",
    "wrangler": "^3.0.0"
  }
}
